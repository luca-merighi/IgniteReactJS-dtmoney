import styled from 'styled-components'

export const FooterContainer = styled.footer`
    margin-top: 3rem;
    padding: 2rem 0;

    text-align: center;

    p {
        color: var(--text-body);
        font-size: 1.15rem;

        span {
            color: var(--blue);
            font-weight: 500;
        }
    }
`